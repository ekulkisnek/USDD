[package]
name = "usdd-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Canonical protocol types, commitments, Merkle utilities, and invariants for USDD"

[features]
default = ["std"]
std = ["sha2/std"]

[dependencies]
sha2.workspace = true
